package com.stuypulse.robot.constants;

public interface Gains {
    public interface superStructure {
        public interface telescope{
            public interface FF{
                double kS = 0;
                double kV = 0;
                double kA = 0; // calculated
                double kG = 0; // calculated
                
            }
        }
    }
}